{
  "name": "jobportal-root",
  "version": "1.0.0",
  "description": "Root package for deploying frontend and backend together on Render",
  "scripts": {
    "build": "npm run build --prefix frontend",
    "install-backend": "npm install --prefix backend",
    "install-frontend": "npm install --prefix frontend",
    "render-postbuild": "npm run build",
    "render-build": "npm run install-backend && npm run install-frontend && npm run render-postbuild",
    "start": "npm start --prefix backend"
  }
}
